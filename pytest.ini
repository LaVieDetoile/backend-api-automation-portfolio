[pytest]
addopts =
    --strict-markers
    --html=reports/api-test-report.html
    --self-contained-html
    -ra
testpaths = tests
pythonpath = .
markers =
    smoke: fast critical API checks
    regression: broader API regression coverage
    negative: error handling and authorization checks
